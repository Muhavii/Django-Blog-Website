{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "bash -c 'python manage.py migrate && python manage.py create_superuser --noinput --username=admin --email=griffinsmuhavi@gmail.com || true && python manage.py collectstatic --noinput && gunicorn myproject.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --timeout 120'"
  }
}
